

# How to build
## Install dependencies
### Ubuntu / Debian

```bash
sudo apt install libboost-all-dev libsqlite3-dev libsodium-dev
```

### Arch / Manjaro

```bash
sudo pacman -S boost libsodium sqlite3
```

## Build

```bash
make build-debug
```
